 Webmin module which manages and audits NFSv4 ACLs on ZFS datasets and paths. Applies baseline ACLs, adds/removes user ACEs, and can enforce recommended ZFS ACL properties. Tested on FreeBSD. There is also a CLI and GUI version as .sh script available under acl_manager repo if you prefer. 
